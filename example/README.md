# Tsai UI component catalog

Interactive Android, iOS, and web catalog that consumes only the public
`tsai_ui` API.

```bash
flutter run -d chrome
```

The catalog currently covers both themes, all button variants and sizes,
loading and disabled states, and buttons with or without a leading icon.
